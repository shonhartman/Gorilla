{
  "name": "gorilla",
  "version": "0.1.0",
  "description": "Multi-brand social marketing hub — GaryVee method, OpenClaw orchestration, Meta API integrations",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node src/index.js",
    "agent": "openclaw run agent/main.yaml",
    "lint": "eslint src/",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "js-yaml": "^4.1.0",
    "openclaw": "^0.5.0",
    "undici": "^7.3.0"
  },
  "devDependencies": {
    "eslint": "^9.18.0"
  }
}
